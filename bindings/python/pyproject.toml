[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lightrt-c"
version = "0.1.0"
description = "Python bindings for the LightRT C11 CPU callback BVH"
requires-python = ">=3.8"
license = { text = "Apache-2.0" }

